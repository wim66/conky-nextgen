-- disk — generated by the NextGen Designer
-- Conky Manager triplet: disk.conf + disk.lua + disk.png

conky.config = {
    background = true,
    out_to_x = true,
    double_buffer = true,
    use_xft = true,
    font = 'Sans:size=10',
    own_window_class = 'Conky',
    own_window_title = 'Conky Disk',
    own_window = true,
    own_window_type = 'normal',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    alignment = 'top_left',
    update_interval = 1,
    total_run_times = 0,
    minimum_width = 340,
    minimum_height = 135,
    gap_y = 470,
    border_inner_margin = 0,
    border_outer_margin = 0,
    border_width = 0,
    lua_load = 'disk.lua',
    lua_draw_hook_pre = 'conky_core_main',
    lua_mouse_hook = 'conky_on_mouse',
    lua_shutdown_hook = 'conky_cleanup',
}

conky.text = [[

]]
