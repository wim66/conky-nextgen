-- nowplaying — Now Playing widget (MPRIS via playerctl)
-- Conky Manager triplet: music.conf + music.lua + music.png

conky.config = {
    background = true,
    double_buffer = true,
    use_xft = true,
    font = 'Sans:size=10',
    own_window = true,
    own_window_type = 'normal',
    own_window_hints = 'below,sticky,skip_taskbar,skip_pager,undecorated',
    own_window_class = 'Conky',
    alignment = 'top_left',
    gap_x = 356,
    gap_y = 245,
    update_interval = 1,
    total_run_times = 0,
    minimum_width = 340,
    minimum_height = 120,
    border_inner_margin = 0,
    border_outer_margin = 0,
    border_width = 0,
    lua_load = 'now-playing.lua',
    lua_draw_hook_pre = 'conky_core_main',
    lua_mouse_hook = 'conky_on_mouse',
    lua_shutdown_hook = 'conky_cleanup',
}

conky.text = [[
${execpi 5 sh/0_fetch_all.sh nowplaying >/dev/null 2>&1}
]]
